func main() {
    var x float64
    var a int
    var y float64
    var activo bool
    var valido bool
    var numero float64
    var resultado int
    var mensaje string

   
    numero = 3.1416

    a = 7
    y = 3.5 * 2.0

    activo = true
    valido = false

    if activo && valido || !(x == 10) {
        fmt.Println("Condición compuesta verdadera", numero)
    } else {
        fmt.Println("Condición compuesta falsa")
    }

    for mensaje {
    }

    for x < 15 && y >= 7.0 {
        fmt.Println("Iterando x:", x)
        x = x + 1
        y = y - 0.5
    }
    fmt.Println("Programa finalizado.")
}